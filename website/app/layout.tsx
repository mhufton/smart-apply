import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Smart Apply — AI-powered job applications",
  description: "Smart Apply is a Chrome extension that helps you tailor your CV and cover letter to any job in seconds. Bring your own Anthropic API key. Your data never leaves your browser.",
  openGraph: {
    title: "Smart Apply — AI-powered job applications",
    description: "Tailored CVs and cover letters in seconds. Bring your own API key. 100% private.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
